define service {
    host_name               localhost
    service_description     C-Compiler-App-Health
    check_command           check_http!-H host.docker.internal -p 1132 -u /actuator/health
    check_interval          1
    retry_interval          1
    max_check_attempts      3
}